export const publish = data => {
  return `Are you sure you want to publish this ${data}?`
}

export const publishChanges = data => {
  return `Do you want to publish changes to this ${data}?`
}

export const deleteContribution = data => {
  return `Are you sure you want to delete this ${data}?`
}

export const draft = data => {
  return `This ${data} has been saved as draft!`
}
